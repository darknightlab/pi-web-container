# Base bind port and exported port (host side).
PI_WEB_PORT=8504
# Keep localhost by default; use a VPN / tunnel / trusted proxy for remote access.
PI_WEB_BIND_ADDR=127.0.0.1
# npm version/range for @jmfederico/pi-web, e.g. latest or 1.202608.1
PI_WEB_VERSION=latest
